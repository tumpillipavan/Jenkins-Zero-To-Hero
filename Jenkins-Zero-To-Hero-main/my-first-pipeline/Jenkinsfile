pipeline {
  agent {
    docker {
      image 'node:16-alpine'
      args '-v /c/jenkins/workspace:/workspace'
    }
  }
  stages {
    stage('Test') {
      steps {
        bat 'node --version'
      }
    }
  }
}


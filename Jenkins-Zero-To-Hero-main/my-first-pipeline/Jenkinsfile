pipeline {
  agent {
    docker {
      image 'node:16-alpine'
      args '-v C:/ProgramData/Jenkins/.jenkins/workspace:/workspace' // Absolute path here
    }
  }
  stages {
    stage('Test') {
      steps {
        bat 'node --version'
      }
    }
  }
}

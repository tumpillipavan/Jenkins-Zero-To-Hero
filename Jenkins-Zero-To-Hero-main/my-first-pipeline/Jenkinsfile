pipeline {
  agent {
    docker {
      image 'node:16-alpine'
      args '-v /c/ProgramData/Jenkins/.jenkins/workspace/Zero-to-hero:/workspace'  // mapping Windows path to Unix style
    }
  }
  stages {
    stage('Test') {
      steps {
        sh 'node --version'
      }
    }
  }
}


pipeline {
  agent any
  stages {
    stage('Test in Docker') {
      steps {
        // Run node:16-alpine with an explicit Windows-style mount
        bat """
          docker run --rm ^
            -v C:\\ProgramData\\Jenkins\\.jenkins\\workspace\\my-first-pipleline:/workspace ^
            -w /workspace ^
            node:16-alpine ^
            node --version
        """
      }
    }
  }
}

